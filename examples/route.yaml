defaults:
  api_key: ${OPENROUTER_API_KEY}
  base_url: https://openrouter.ai/api/v1
  format: openai

routes:
  fast-kimi:
    type: passthrough
    target: moonshotai/kimi-k2.6

  smoke-test:
    type: noop
